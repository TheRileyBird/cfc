---
import { Image } from 'astro:assets';
import cfcLogo from '../assets/images/cfc-logo.png';

export interface Props {
  position?: 'fixed' | 'sticky';
}
const { position = 'fixed' } = Astro.props;
const accountHref = 'https://cfcskincare.myshopify.com/account/login';
---

<div
  id="site-nav"
  x-data="{ open: false, scrolled: false, pastMarquee: false }"
  x-init="
    const hero = document.getElementById('hero');
    if (!hero) {
      scrolled = true;
      pastMarquee = true;
    } else {
      const update = () => {
        const y = window.scrollY;
        scrolled = y > 60;
        pastMarquee = y > hero.offsetHeight;
      };
      window.addEventListener('scroll', update, { passive: true });
      update();
    }
  "
  class={`top-0 inset-x-0 z-50 ${position === 'sticky' ? 'sticky' : 'sticky lg:fixed'}`}
  :class="scrolled ? 'scrolled' : ''"
>

  <!-- Announcement bar — fades to full opacity on scroll, then collapses over -->
  <div
    class="overflow-hidden announcement-bar"
    :style="`background: oklch(13% 0.010 255 / ${scrolled || window.innerWidth < 1024 ? '1' : '0.6'}); max-height: ${pastMarquee ? '0' : '45px'};`"
  >
    <div class="text-center py-2.5 px-6">
      <a
        href="/shop"
        class="font-mono text-[10px] tracking-[0.18em] uppercase text-gold-light hover:underline underline-offset-2 transition-all duration-200"
      >
        Subscribe &amp; Save 30% Off &mdash; Shop Now
      </a>
    </div>
  </div>

  <!-- Main nav bar -->
  <nav
    class="relative flex items-center transition-[height] duration-300 ease-in-out max-w-[1440px] mx-auto px-10"
    :class="scrolled ? 'h-[56px]' : 'h-[72px]'"
  >

    <!-- Logo — always visible -->
    <a href="/" class="flex items-center gap-3 shrink-0 z-10">
      <Image
        src={cfcLogo}
        alt="CFC Skincare"
        width={40}
        height={40}
        densities={[1, 2, 3]}
        class="w-10 h-10 rounded-full object-cover shrink-0 transition-transform duration-[600ms] ease-[cubic-bezier(0.16,1,0.3,1)]"
      />
      <span
        class="hidden lg:block font-mono text-[11px] tracking-[0.22em] uppercase transition-colors duration-300"
        :class="scrolled ? 'text-ink' : 'text-white'"
      >CFC Skincare</span>
    </a>

    <!-- Desktop nav links — only shown once scrolled -->
    <ul
      x-show="scrolled"
      x-transition:enter="transition ease-out duration-300"
      x-transition:enter-start="opacity-0"
      x-transition:enter-end="opacity-100"
      x-transition:leave="transition ease-in duration-150"
      x-transition:leave-start="opacity-100"
      x-transition:leave-end="opacity-0"
      class="hidden lg:flex absolute left-1/2 -translate-x-1/2 items-center gap-10 list-none"
    >
      {[
        ['Shop', '/shop'],
        ['The System', '/systems'],
        ['About', '/about'],
        ['Contact', '/contact'],
      ].map(([label, href]) => (
        <li>
          <a
            href={href}
            class="nav-link font-mono text-[10px] tracking-[0.18em] uppercase text-ink-soft hover:text-ink transition-colors duration-200 relative leading-none pb-[3px]"
          >
            {label}
          </a>
        </li>
      ))}
    </ul>

    <!-- Desktop right side -->
    <div class="hidden lg:flex items-center ml-auto z-10 gap-5">

      <!-- Icons + divider — only shown once scrolled -->
      <div
        x-show="scrolled"
        x-transition:enter="transition ease-out duration-300"
        x-transition:enter-start="opacity-0"
        x-transition:enter-end="opacity-100"
        x-transition:leave="transition ease-in duration-150"
        x-transition:leave-start="opacity-100"
        x-transition:leave-end="opacity-0"
        class="flex items-center gap-5"
      >
        <button
          @click="$store.search.isOpen ? $store.search.close() : $store.search.open()"
          class="text-ink-soft hover:text-ink transition-colors"
          aria-label="Search"
        >
          <svg class="w-[17px] h-[17px]" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5">
            <circle cx="11" cy="11" r="8"/>
            <path stroke-linecap="round" d="m21 21-4.35-4.35"/>
          </svg>
        </button>
        <a href={accountHref} class="text-ink-soft hover:text-ink transition-colors" aria-label="Account">
          <svg class="w-[17px] h-[17px]" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5">
            <path stroke-linecap="round" stroke-linejoin="round" d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
            <circle cx="12" cy="7" r="4"/>
          </svg>
        </a>
        <button
          @click="$store.cart.open()"
          class="relative text-ink-soft hover:text-ink transition-colors"
          aria-label="Cart"
        >
          <svg class="w-[17px] h-[17px]" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"/>
            <line x1="3" y1="6" x2="21" y2="6"/>
            <path stroke-linecap="round" d="M16 10a4 4 0 0 1-8 0"/>
          </svg>
          <span
            x-show="$store.cart.totalQuantity > 0"
            x-text="$store.cart.totalQuantity"
            class="absolute -top-2 -right-2 w-4 h-4 flex items-center justify-center bg-gold text-white text-[9px] font-bold rounded-full leading-none"
          ></span>
        </button>
        <div class="w-px h-5 bg-rule"></div>
      </div>

      <!-- Shop Now — always visible on desktop -->
      <a
        href="/shop"
        class="inline-flex items-center font-mono text-[10px] tracking-[0.18em] uppercase transition-colors duration-200 px-[22px] py-[10px]"
        :class="scrolled ? 'bg-ink text-white hover:bg-gold-deep' : 'bg-[rgba(255,255,255,0.1)] text-white border border-[rgba(255,255,255,0.25)] hover:bg-gold hover:border-gold hover:text-ink'"
      >
        Shop Now
      </a>
    </div>

    <!-- Mobile right — always dark (nav is sticky on mobile, never overlays hero) -->
    <div class="lg:hidden flex items-center gap-4 ml-auto">
      <button
        @click="$store.search.isOpen ? $store.search.close() : $store.search.open()"
        class="text-ink-soft hover:text-ink transition-colors"
        aria-label="Search"
      >
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5">
          <circle cx="11" cy="11" r="8"/>
          <path stroke-linecap="round" d="m21 21-4.35-4.35"/>
        </svg>
      </button>
      <button
        @click="$store.cart.open()"
        class="relative text-ink-soft hover:text-ink transition-colors"
        aria-label="Cart"
      >
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"/>
          <line x1="3" y1="6" x2="21" y2="6"/>
          <path stroke-linecap="round" d="M16 10a4 4 0 0 1-8 0"/>
        </svg>
        <span
          x-show="$store.cart.totalQuantity > 0"
          x-text="$store.cart.totalQuantity"
          class="absolute -top-2 -right-2 w-4 h-4 flex items-center justify-center bg-gold text-white text-[9px] font-bold rounded-full leading-none"
        ></span>
      </button>
      <button
        @click="open = !open"
        class="p-1.5 text-ink-soft hover:text-ink transition-colors"
        aria-label="Menu"
      >
        <span x-show="!open" class="block space-y-1.5">
          <span class="block h-px w-6 bg-current"></span>
          <span class="block h-px w-4 bg-current ml-auto"></span>
        </span>
        <svg x-show="open" class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M6 18L18 6M6 6l12 12"/>
        </svg>
      </button>
    </div>
  </nav>

  <!-- Mobile drawer -->
  <div
    x-show="open"
    x-transition:enter="transition ease-out duration-200"
    x-transition:enter-start="opacity-0 -translate-y-2"
    x-transition:enter-end="opacity-100 translate-y-0"
    x-transition:leave="transition ease-in duration-150"
    x-transition:leave-start="opacity-100"
    x-transition:leave-end="opacity-0"
    class="lg:hidden bg-white border-t border-rule px-6 pb-8 pt-4"
  >
    {[
      ['Shop', '/shop'],
      ['The System', '/systems'],
      ['About', '/about'],
      ['Contact', '/contact'],
    ].map(([label, href]) => (
      <a
        href={href}
        @click="open=false"
        class="block py-3.5 font-mono text-[11px] tracking-[0.25em] uppercase text-ink-soft hover:text-ink border-b border-rule last:border-0 transition-colors"
      >
        {label}
      </a>
    ))}
    <div class="mt-5 flex gap-3">
      <a
        href={accountHref}
        class="flex-1 text-center py-3 border border-rule text-ink font-mono text-[10px] tracking-[0.18em] uppercase hover:border-ink transition-colors"
      >
        Account
      </a>
      <a
        href="/shop"
        class="flex-1 text-center py-3 bg-ink text-white font-mono text-[10px] tracking-[0.18em] uppercase hover:bg-gold-deep transition-colors"
      >
        Shop Now
      </a>
    </div>
  </div>

  <!-- Search panel -->
  <div
    x-show="$store.search.isOpen"
    x-cloak
    x-transition:enter="transition ease-out duration-200"
    x-transition:enter-start="-translate-y-3 opacity-0"
    x-transition:enter-end="translate-y-0 opacity-100"
    x-transition:leave="transition ease-in duration-150"
    x-transition:leave-start="opacity-100 translate-y-0"
    x-transition:leave-end="-translate-y-3 opacity-0"
    @keydown.escape.window="$store.search.close()"
    class="bg-white border-b border-rule shadow-strong"
  >
    <div class="max-w-7xl mx-auto px-6 lg:px-10">
      <div class="flex items-center gap-3 py-4">
        <svg class="w-4 h-4 text-ink-soft shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5">
          <circle cx="11" cy="11" r="8"/>
          <path stroke-linecap="round" d="m21 21-4.35-4.35"/>
        </svg>
        <input
          type="text"
          placeholder="Search products…"
          x-model="$store.search.query"
          @input.debounce.300ms="$store.search.doSearch()"
          x-effect="if ($store.search.isOpen) $nextTick(() => $el.focus())"
          class="flex-1 text-sm text-ink placeholder-ink-soft border-0 outline-none bg-transparent font-sans"
          autocomplete="off"
        />
        <button
          @click="$store.search.close()"
          class="shrink-0 text-ink-soft hover:text-ink transition-colors p-1"
          aria-label="Close search"
        >
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/>
          </svg>
        </button>
      </div>

      <div x-show="$store.search.query.length > 0" class="pb-5">
        <div x-show="$store.search.isLoading" class="flex items-center justify-center py-6">
          <svg class="animate-spin w-5 h-5 text-gold" fill="none" viewBox="0 0 24 24">
            <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"/>
            <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"/>
          </svg>
        </div>
        <div
          x-show="!$store.search.isLoading && $store.search.results.length === 0"
          class="py-5 text-center text-sm text-ink-soft font-sans"
        >
          No results for &ldquo;<span x-text="$store.search.query" class="font-medium text-ink"></span>&rdquo;
        </div>
        <div
          x-show="!$store.search.isLoading && $store.search.results.length > 0"
          class="grid grid-cols-2 sm:grid-cols-4 lg:grid-cols-8 gap-3"
        >
          <template x-for="product in $store.search.results" :key="product.id">
            <a
              :href="`https://www.cfcskincare.shop/products/${product.handle}`"
              @click="$store.search.close()"
              class="group block"
            >
              <div class="aspect-square overflow-hidden bg-parchment">
                <img
                  :src="product.imageUrl"
                  :alt="product.imageAlt || product.title"
                  class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
                />
              </div>
              <p class="font-mono text-[10px] tracking-[0.1em] uppercase mt-2 text-ink group-hover:text-gold transition-colors line-clamp-2 leading-snug" x-text="product.title"></p>
              <p class="font-mono text-[10px] text-ink-soft mt-0.5" x-text="$store.search.formatPrice(product.price)"></p>
            </a>
          </template>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  #site-nav {
    transition: background 0.5s ease, backdrop-filter 0.5s ease, border-color 0.5s ease;
  }

  #site-nav.scrolled {
    background: rgba(253, 251, 247, 0.95);
    backdrop-filter: blur(16px);
    border-bottom: 1px solid oklch(88% 0.006 72);
  }

  .nav-link {
    position: relative;
  }
  .nav-link::after {
    content: '';
    position: absolute;
    bottom: -3px;
    left: 0;
    width: 0;
    height: 1px;
    background: oklch(64% 0.115 72);
    transition: width 0.3s cubic-bezier(0.16,1,0.3,1);
  }
  .nav-link:hover::after {
    width: 100%;
  }

  .announcement-bar {
    transition: max-height 0.6s cubic-bezier(0.16, 1, 0.3, 1), background 0.5s ease;
  }
</style>
